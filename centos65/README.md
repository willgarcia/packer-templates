Template CentOS
--------------------------

This template provides:

* a minimal 64-bit CentOS 6.5

Build:
```shell
$ make centos65-vbox
```

Edit `variables.json` to customize the box.

Usage:
```shell
$ vagrant up
```
